$(function () {
  /* ************************
   * about, 스와이퍼
   ************************ */
  const interviewsSwiper = new Swiper(".swiper", {
    // Optional parameters
    direction: "vertical",
    autoplay: true,
    loop: true,
    slidesPerView: 3,
    spaceBetween: 30,
    centeredSlides: true,
  });

  /* ************************
   * about, passion-for-work
   ************************ */
});
